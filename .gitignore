########################################
# 🔧 General OS and Editor Files
########################################
.DS_Store
Thumbs.db
.Spotlight-V100
.Trashes
*.log
*.bak
*.swp
*.pid
article/
input/
prompts/
images/
lics/
git_commit.sh
FOLDER_TREE.md

########################################
# 💻 IDE & Editor Configs
########################################
.vscode/
.idea/
.spyderproject
.spyproject
.ropeproject

########################################
# 🐍 Python
########################################
__pycache__/
*.py[cod]
*.pyo
*$py.class
*.so
*.egg
*.egg-info/
.installed.cfg
.Python
env/
venv/
ENV/
env.bak/
venv.bak/
.venv/
.python-version
pip-log.txt
pip-delete-this-directory.txt
*.manifest
*.spec
*.mo
*.pot
*.cover
*.out

########################################
# 📦 Python Build and Packaging
########################################
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# lib/
lib64/
parts/
sdist/
var/
wheels/
target/

########################################
# 🧪 Python Testing and Coverage
########################################
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
.hypothesis/
.pytest_cache/
.pyre/
.mypy_cache/
.dmypy.json
dmypy.json

########################################
# 🧬 Python Frameworks
########################################
local_settings.py
db.sqlite3
instance/
.webassets-cache
.scrapy
site/
celerybeat-schedule
profile_default/
ipython_config.py

########################################
# 📦 Node.js
########################################
node_modules/
npm-debug.log*

########################################
# 📦 Containers (Podman is the canonical runtime)
########################################
*.tar
compose.override.yml
backups/
data/
*.hclic
vault-tls/
.secrets/

########################################
# 🌍 Environment and Config Files
########################################
.env
.env.*
!.env.example

########################################
# 🛠️ Terraform
########################################
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
*.backup
.terraform.lock.hcl
*.log
crash.log

########################################
# 🛠️ Prompt 28 — Terraform provider skeleton build output
########################################
terraform/arcanium-provider/terraform-provider-arcanium

########################################
# 🎨 Prompt 00 — Impeccable's platform-specific compiled binary.
# SKILL.md / reference/*.md / the bundled JS are portable and committed
# so the skill works for any collaborator without re-running the
# installer; the native darwin-arm64 (or any other platform's) binary
# would not even run on a different OS/arch and is re-fetched by
# `npx impeccable install` on any machine that needs it, same reasoning
# as the Terraform provider binary above.
########################################
**/skills/impeccable/scripts/bin/

# Claude Code's own convention: settings.local.json is per-developer/
# per-machine, never the shared/committed settings.json.
.claude/settings.local.json
justfile
# tfvars often carry real credentials — commit .tfvars.example instead
*.tfvars
!*.tfvars.example
!*.tfvars.sample

########################################
# 🔐 Private Keys & Certificates
########################################
*.pem
*.key
*.cert
*.crt
*.pfx
*.p12
*.p8
*.jks
# SSH keys
id_rsa
id_rsa.pub
id_dsa
id_dsa.pub
id_ecdsa
id_ecdsa.pub
id_ed25519
id_ed25519.pub
# Common key/cert directories
tls/
certs/
private/
secrets/
.secrets/
credentials/

########################################
# 🏛️ HashiCorp Vault
########################################
# Vault storage backend — contains keyring and master encryption keys
vault/vault-data/
vault-data/
# Vault init files — contain root tokens and unseal keys
vault-init.txt
vault.init
init.txt

########################################
# 🍪 Sessions, Cookies & Auth State
########################################
cookies.txt
*cookies*.txt
*.cookies
*-state.json
*-storage-state.json
sessions/
.auth/
.playwright/

########################################
# ☁️ Cloud Provider Credentials
########################################
# AWS
.aws/credentials
# GCP
application_default_credentials.json
*-service-account.json
*-sa-key.json
service_account*.json
# HCP / generic cloud
*.token
token.txt

########################################
# 🗄️ Database
########################################
*.sqlite
*.sqlite3
.pgpass
pgpass

########################################
# ☸️ Kubernetes
########################################
kubeconfig
.kubeconfig
kube/config


# ── Added by commit_gh --harden ─────────────────────────────
*.env
.vault.*
*.vault
*.jsonl
input/*
!input/.gitkeep
output/*
!output/.gitkeep
compose/observability/prometheus/vault-token

# state/ capture staging — an in-progress or failed capture-state.sh run
# (Prompt 00 atomic capture: built in .tmp, only renamed into
# state/baselines/ once validated). Never a finalized baseline; must never
# be swept into a commit by accident.
state/.capture-*.tmp/

########################################
# 🎨 Prompt 00 — frontend design toolchain / quality gate
########################################
# Playwright CLI's own session/snapshot/console-log working files and
# generated visual-development artifacts (before/after screenshots) — never
# durable configuration or reports, regenerated on every run.
arcanium/ui/.playwright-cli/
arcanium/ui/.artifacts/
arcanium/ui/test-results/
arcanium/ui/playwright-report/
